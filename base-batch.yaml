---
feedback_level: FULL

score_options:
  decimal_places: 0
  mode: SUM_SUBTASK_BEST
  type: GROUP_MIN

time_limit: 1.0s
memory_limit: 256MiB

task_type: BATCH

cpp_config:
  gcc_args: -O2 -std=c++17 -static -s -Wno-unused-result

latex_config:
  additional_files: []
